/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FixedPipe } from './fixed.pipe';

describe('Pipe: Fixed', () => {
  it('create an instance', () => {
    let pipe = new FixedPipe();
    expect(pipe).toBeTruthy();
  });
});
