/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AppObservableCountdownComponent } from './app-observable-countdown.component';

describe('Component: AppObservableCountdown', () => {
  it('should create an instance', () => {
    let component = new AppObservableCountdownComponent();
    expect(component).toBeTruthy();
  });
});
